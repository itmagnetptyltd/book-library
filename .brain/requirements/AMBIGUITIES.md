# Ambiguities — Book Library

Open questions arising from the client brief (Client brief, ITMagnet - Test,
email 2026-08-19, quoted verbatim in `.brain/requirements/BRIEF.md`).

Each heading is a question for the client, and appears word for word in the
`ambiguities` list of every requirement it affects in `book.yaml`. The two files
are one list seen twice. **None of these may be closed by anyone choosing a
reading** — only the client's answer closes one.

---

## No open questions

All **25** questions raised at decomposition have been answered by the client,
in two replies recorded in `ANSWERS.md` on 2026-08-21:

- **21** were closed by the first reply.
- **4** remained open after it — currency, screen sizes, the acceptance bar for
  appearance, and the Node.js version — because that reply deferred or restated
  them rather than answering. The second reply settled all four.

Their answers are now acceptance criteria in `book.yaml`, and every requirement
in that file is `agreed`.

Answered questions are removed from this file rather than marked closed. The
wording of each question, and the client's answer to it, is kept in `ANSWERS.md`.

**This file being empty is not the end of it.** A scope change, a new brief, or
delivered work the client reacts to will raise new questions — `/decompose` and
`/find-variation` both write back here.
